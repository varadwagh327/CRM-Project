# ==========================================================
# ? APPLICATION SETTINGS
# ==========================================================
spring.application.name=crm
server.port=8080


# ==========================================================
# ? DATABASE CONFIGURATION (MySQL)
# ==========================================================
spring.datasource.url=jdbc:mysql://localhost:3306/crm?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=varadwagh9
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# (Optional) prevent Hibernate from auto-creating sequences for MySQL
spring.jpa.properties.hibernate.id.new_generator_mappings=false


# ==========================================================
# ? JWT CONFIGURATION
# ==========================================================
app.secretkey=crm-secret-key-12345678910-crm-secret-key-12345678910
jwt.issuer=crmService


# ==========================================================
# ? EMAIL CONFIGURATION
# ==========================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bhavarthnagavkar@gmail.com
spring.mail.password=zdji llbd szri bhru

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


# ==========================================================
# ? LOGGING / DEBUGGING
# ==========================================================
# Show SQL in logs (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application log level
logging.level.com.crm=INFO
